<?xml version="1.0" encoding="UTF-8"?>
<!-- Spring MVC config — view resolver, component scan, and interceptor -->

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- scan for @Controller, @Service, @Repository etc. -->
    <context:component-scan base-package="com.university" />

    <!-- enables @GetMapping, @PostMapping etc. -->
    <mvc:annotation-driven />

    <!-- maps controller return strings to JSP files in /WEB-INF/views/ -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <mvc:interceptors>
        <bean class="com.university.interceptor.LoggingInterceptor" />
    </mvc:interceptors>

</beans>